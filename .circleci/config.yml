version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     - run: |
         pushd dotnet
         docker build --build-arg VERSION="1.0.$CIRCLE_BUILD_NUM-rc" -t streamsdb/dotnet-driver .
         popd
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
